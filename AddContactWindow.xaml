<Window x:Class="ContactManager.AddContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ContactManager"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        Title="Add contact" Height="300" Width="350" WindowStartupLocation="CenterScreen"
        WindowStyle="None" BorderBrush="Black" BorderThickness="2">

    <Window.Resources>
        <ObjectDataProvider x:Key="GenderEnum" MethodName="GetValues" ObjectType="{x:Type TypeName=System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:Gender"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="Add New Contact" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <Label Content="Name:"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Surname:"/>
        <TextBox Text="{Binding Surname, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Gender:"/>
        <ComboBox ItemsSource="{Binding Source={StaticResource GenderEnum}}" SelectedItem="{Binding Gender}"/>

        <UniformGrid Columns="2" Margin="0,20,0,0">
            <Button Content="OK" Click="Ok_Click" Margin="5"/>
            <Button Content="Cancel" Click="Cancel_Click" Margin="5"/>
        </UniformGrid>
    </StackPanel>
</Window>